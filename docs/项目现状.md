# TaskFlow 项目现状

> 存档日期:2026-09-10 | 代码基线:`main @ 最新`(已推送)

## 1. 系统运行状态

| 层 | 地址 | 状态 |
|---|---|---|
| 前端(Vite dev) | http://localhost:5173 | ✅ 200 |
| 网关 | http://127.0.0.1:8000 | ✅ 路由正常 |
| auth-user-service | :8081 | ✅ |
| task-service | :8082 | ✅ |
| notification-service | :8083 | ✅ |
| stats-service | :8084 | ✅ |
| Nacos | 注册中心 8848 · 控制台 8080(免登录) | ✅ |
| PostgreSQL / Redis / RabbitMQ / nginx(8088) | 5432 / 6379 / 5672 / 8088 | ✅ |

## 2. 数据规模(演示数据)

- 任务 241(含 12 子任务、23 评论、404 时间线、0 附件),状态/优先级/类型/处理人全维度
- 通知 28、邮件记录 23
- app_user 181(大量为历次验收/压测产生的禁用测试账号,待整理)

## 3. 里程碑进度(M0–M6 全部完成)

| 里程碑 | 状态 |
|---|---|
| M0 工程脚手架 / M1 认证与用户域 / M2 任务核心 / M3 通知与事件链路 / M4 任务扩展 / M5 统计与定时任务 / M6 联调验收与上线 | ✅ 全部完成 |
| PRD 16 条验收 / 54 用例(REST Assured,`tests/acceptance/`) | ✅ 全绿 |
| 性能(1 万行规模) | ✅ 列表 P95 6ms / 统计 8ms / 导入 500 行 0.67s / 导出 1 万行 0.21s |

## 4. 近期迭代要点(2026-09 下旬)

- **bug 修复**:删除任务级联清评论/附件并写审计(`task.delete`);删除带子任务父任务 → 2013 拦截;JWT 加 jti 防同秒 token 复现;权限缓存持久化 + 下游回源自愈(修复 3001 误判)
- **稳定性**:notification/stats 消费失败落死信 DLQ(防 requeue 风暴,毒消息丢弃);mail.failed 告警 30 分钟去重 + 摘要截断(修复 SMTP 未配置刷屏);stats 消费者加 eventId 缺失防护
- **统计口径**:stats 模型对齐 PRD 4.3.2——`p0_unfinished` 列 + `stats_completion_daily` 完成日聚合,overview KPI.p0/趋势 completed 按新口径,rebuild 全量校准
- **通知中心**:`mail.failed` 系统告警中文化、专属邮件图标、默认排除在业务通知外(铃铛/未读角标只计业务,中心页可切"系统告警"查看);铃铛面板视觉重构(未读胶囊/类型标签/两行截断/查看全部)
- **前端 UI**:日历/统计/通知控件统一 Element 默认尺寸(去自绘分段);任务范围筛选 → el-radio-button;页面标题色统一;操作文字链抽公共 `.tf-op-link`;日程更名"日程管理";日历支持选具体日期(跳月+高亮+当日面板);浏览器标签品牌
- **演示数据**:清理压测 1.15 万任务/死链通知;SQL 灌入 214 顶层+14 子任务全维度数据;新增 4 个演示账号(zhangming/zhaoqiang/lihua/wangfang,密码统一 TaskFlow@2026)

## 5. 账号备忘

| 账号 | 角色 | 密码 | 备注 |
|---|---|---|---|
| admin | 系统管理员 | Admin@123 | must_change_password=TRUE,首登强制改密 |
| zhangming / zhaoqiang | 任务管理员 | TaskFlow@2026 | 演示账号 |
| lihua / wangfang | 普通用户 | TaskFlow@2026 | 演示账号(可见性/权限受限) |
| testuser | 普通用户 | TaskFlow@2026 | 早期测试账号 |
| svc_openapi | taskAdmin 服务账号 | (不可登录) | API Key 绑定用 |

## 6. 文档索引(`docs/`)

- 里程碑报告:M0–M6(含 M6.1 验收报告、M6 性能报告)
- 设计类:PRD / 接口设计 / 库表设计 / 架构设计 / UI 规范 / 实施计划 / 本地中间件(本机实测版)
- 运维类:安全核查清单、上线手册、部署手册(deploy/README)
- 走查类:人工走查清单、前端 UI 走查报告、项目现状(本文)

## 7. 遗留待办(上线前)

1. **SMTP 真发信**(个人免费邮箱指南已备:QQ/163 + 授权码;或本地 MailHog)
2. 生产环境:换默认密钥(JWT/DB/Redis/Rabbit/SMTP)、HTTPS(443 配置片段见安全清单)、Compose 真机部署与备份恢复演练、CI(GitHub Actions)
3. 数据卫生:清理历史测试账号(m61*/acc_p1*/越权测试 等)、旧 mail.failed 记录
4. 浏览器人工走查(`docs/人工走查清单.md`)尚未执行
5. UI 工程优化(可选项):空态组件统一 `.tf-empty`、颜色硬编码改 CSS 变量、面板圆角核对
6. 已知限制明细:转派负载漂移靠 rebuild 兜底、KPI 口径按库表设计的简化(见 M5/M6 报告)、限流 fail-open 为刻意选择

## 8. M7 可观测性与配置中心(2026-09-10 落地)

| 能力 | 状态 |
|---|---|
| Actuator 健康与探针 | ✅ 五服务 `/actuator/health`、`/health/readiness`、`/health/liveness` 全 UP,白名单放行 |
| Prometheus 指标 | ✅ `/actuator/prometheus`(JVM/HTTP/进程指标,带 application 标签) |
| 链路追踪 traceId | ✅ 网关生成/透传 `X-Trace-Id` → 下游 MDC → 日志 `[traceId]`,跨服务可 grep 串联 |
| 统一日志 | ✅ 五服务 `logback-spring.xml`(控制台+文件、按天滚动、totalSizeCap=1GB) |
| Nacos 配置中心 | ✅ 两个 dataId(共享 `taskflow-common.yaml` + 服务专属)+ `@RefreshScope` 热刷新(实测改配额即生效) |
| 创建任务幂等 | ✅ `Idempotency-Key`(同 Key 24h 只建一条、并发 409/2014、命中回放、键值 sha256 落 Redis) |
| 运维隐患 | ✅ 删除 11.5GB 历史日志、补日志容量上限 |

**仍未做**:Prometheus/Grafana 实际部署与告警规则、OTel/Jaeger 追踪后端、CI/CD、HTTPS、真 SMTP、Compose 真机演练、历史测试账号清理。
