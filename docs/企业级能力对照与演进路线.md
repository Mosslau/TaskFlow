# TaskFlow 企业级能力对照与演进路线

> 评估日期:2026-09-10 | 基线:`main @ 4c5cb20`
> 图例:✅ 已具备 · 🟡 部分/简化 · ❌ 缺失
> 目的:以"企业级任务管理系统"的通行能力清单为标尺,盘点现状、量化差距、给出分批演进路线与非功能基线。

---

## 1. 评估方法

### 1.1 模块划分

能力清单按 **14 个模块、3 个领域**组织,共 135 个功能点:

| 领域 | 模块 |
|---|---|
| **A. 组织与权限** | ① 身份与组织 ② 权限与安全 ③ 合规与认证 |
| **B. 业务与协作** | ④ 任务与工作流 ⑤ 协作与内容 ⑥ 计划与日程 ⑦ 通知与集成 ⑧ 报表与度量 ⑨ 搜索与知识 ⑩ 用户体验 |
| **C. 平台与生态** | ⑪ AI 与智能化 ⑫ 平台与运维(SRE·供应链) ⑬ 开放与生态 ⑭ 产品化与运营 |

### 1.2 评分口径

- 单模块得分 = (✅×1 + 🟡×0.5) ÷ 该模块功能点数 × 100%
- **综合企业就绪度** = 14 个模块得分算术平均
- **门槛项**:企业采购/IT 安全评审必问,缺失会导致无法入库(第 4 节 P0)
- **加分项**:影响采纳率与效率,不构成准入障碍(P1)
- **规模化项**:客户与数据量增长后才需要(P2)

### 1.3 口径说明

早期版本仅统计"功能地基"且口径偏松,给出过 40% 的综合分。本版把 AI、合规认证、SRE 与供应链、用户体验、产品化运营、度量与自动化并入评估,并统一 ✅=1 / 🟡=0.5 的算法,因此数值不可直接比较——本版结论更能代表**企业就绪度**。

---

## 2. 能力对照表

### 2.1 领域 A:组织与权限

#### ① 身份与组织 —— 25%

| 功能 | 现状 | 说明 |
|---|---|---|
| 账号体系(登录/改密/失败锁定/强制改密) | ✅ | BCrypt、5 次失败锁 15 分钟、首登强制改密、JWT + jti 黑名单 |
| SSO(SAML2/OIDC/LDAP/企业微信·钉钉·飞书) | ❌ | 企业不会为单系统维护独立账号体系 |
| 组织架构(多层级部门树/汇报线/岗位) | 🟡 | 仅单层"部门",无层级与岗位 |
| 用户生命周期(开通/停用/离职交接/批量导入) | 🟡 | 有新增/停用/角色指派;无交接与批量 |
| 多租户 / 多组织隔离 | ❌ | 单租户,业务表无 tenant_id |
| 外部协作方(Guest 账号) | ❌ | — |
| 代理与委托(请假期间代办) | ❌ | — |
| HR 同步开户/销户(SCIM / LDAP 同步) | ❌ | — |

#### ② 权限与安全 —— 44%

| 功能 | 现状 | 说明 |
|---|---|---|
| RBAC 角色 × 权限点 + 矩阵即改即存 | ✅ | 3 角色 × 14 权限点、42 行矩阵、改后下一请求生效 |
| 数据范围(本人/本部门/部门树/指定/全部) | 🟡 | 靠可见性规则硬编码,不可配置 |
| 字段级权限(敏感字段按角色隐藏/只读) | ❌ | — |
| 临时授权与到期回收 | ❌ | — |
| 职责分离(提交≠验收、审批链互斥) | 🟡 | 创建人验收、非处理人不可提交;无通用 SoD |
| 全量审计(谁在何时改了什么) | 🟡 | 仅权限矩阵变更 + 任务删除;未覆盖全量动作 |
| 安全防护(限流/越权/防探测/密码策略) | ✅ | 网关限流、统一 403 不泄露存在性、API Key sha256 |
| 敏感字段加密与密钥托管(KMS/Vault) | ❌ | 仅密码 BCrypt,其余明文 |

#### ③ 合规与认证 —— 13%

| 功能 | 现状 | 说明 |
|---|---|---|
| 等保 2.0 / SOC 2 / ISO 27001 对齐 | ❌ | 需制度 + 技术证据 + 第三方审计 |
| 第三方渗透测试与安全评估报告 | ❌ | 项目内已有安全核查清单,但非第三方报告 |
| 数据分级分类与脱敏策略 | ❌ | 字段与附件均未分级 |
| 审计日志防篡改(哈希链 / WORM) | 🟡 | 审计只增不改,但无完整性校验与防篡改 |
| 法务保全(Legal Hold) | ❌ | — |
| 数据主体删除(DSAR)与导出审计 | ❌ | 保留策略已实现,删除/保全未实现 |
| 权限定期复核(Access Review) | ❌ | 企业要求季度复核"谁有什么权限" |
| 数据本地化 / 跨境合规 | 🟡 | 支持单机本地化部署;跨境策略未定义 |

### 2.2 领域 B:业务与协作

#### ④ 任务与工作流 —— 25%

| 功能 | 现状 | 说明 |
|---|---|---|
| 任务 CRUD + 状态机(8 动作) | ✅ | 受理/进度/提交验收/通过/驳回/转派/优先级/到期/归档,非法流转 2002 |
| 优先级 / 类型 / 标签 | 🟡 | P0-P3 + 6 类型;无标签体系 |
| 可配置工作流(自定义状态/流转/审批节点) | ❌ | 状态机硬编码,难以适配第二个部门的流程 |
| 自定义字段与表单 | ❌ | — |
| 子任务与任务依赖 | 🟡 | 一级子任务 ✅;前置依赖/阻塞关系 ❌ |
| 周期 / 重复任务 | ❌ | — |
| SLA 与升级催办 | 🟡 | 到期与逾期提醒;无逐级升级、无记分 |
| 批量操作(指派/改期/归档/删除) | ❌ | — |
| 工时与成本核算 | ❌ | — |
| 模板库(任务/项目模板) | ❌ | — |

#### ⑤ 协作与内容 —— 33%

| 功能 | 现状 | 说明 |
|---|---|---|
| 评论(基础) | ✅ | 可见即可评,评论者排除通知 |
| @提及与内外部可见性区分 | ❌ | — |
| 附件上传/下载/删除(白名单 + 限额) | ✅ | 20MB、每任务 10 个、14 类白名单、UUID 落盘名 |
| 附件版本 / 在线预览 / 水印 | ❌ | 且当前存本地磁盘(见⑫无状态化) |
| 操作时间线(只增不改) | ✅ | — |
| Wiki / 知识库 | ❌ | — |
| 会议纪要与行动项转化 | ❌ | — |
| 通用审批中心(请假/报销/采购) | ❌ | — |
| 电子签名 / 验收凭证 | ❌ | — |

#### ⑥ 计划与日程 —— 33%

| 功能 | 现状 | 说明 |
|---|---|---|
| 月历 / 日程视图 | ✅ | 按到期日聚合(含子任务)、选日期跳月、当日面板 |
| 甘特图 / 时间线 / 里程碑 | ❌ | — |
| 项目 / 项目集容器 | ❌ | 任务彼此孤立,无上层容器 |
| 日历双向同步(Exchange / CalDAV) | ❌ | — |
| 个人工作台(我的待办/今日要务) | 🟡 | 列表筛选可近似 |
| 容量规划(FTE / 工时预测) | 🟡 | 仅当前未完成负载快照 |

#### ⑦ 通知与集成 —— 13%

| 功能 | 现状 | 说明 |
|---|---|---|
| 站内消息 + 邮件双通道 | ✅ | 8 类事件、未读角标、失败重试与告警、系统告警隔离 |
| IM 通知(企业微信/钉钉/飞书) | ❌ | — |
| 短信 / 移动推送 | ❌ | — |
| Webhook / 外部事件订阅 | ❌ | 内部有 MQ 事件,未对外开放 |
| 第三方集成(Git/Jenkins/CRM/HR) | ❌ | — |
| 通知偏好 / 免打扰 / 聚合摘要 | ❌ | — |
| 邮件入站转任务(inbound email) | ❌ | — |
| 企业门户嵌入 / iPaaS | ❌ | — |

#### ⑧ 报表与度量 —— 28%

| 功能 | 现状 | 说明 |
|---|---|---|
| 预置看板(KPI + 图表) | ✅ | 6 KPI + 趋势/状态/优先级/负载,四区间,口径对齐 PRD |
| 自定义报表 / 透视 | ❌ | — |
| 定时报表(周报/月报邮件) | ❌ | — |
| 组织维度绩效(部门/团队聚合) | 🟡 | 有人员负载,无部门维度聚合 |
| OKR / KPI 目标对齐与上卷 | ❌ | — |
| 导出与 BI / 数仓对接 | 🟡 | CSV 导出 ✅;只读库/BI 未接 |
| 阈值预警(逾期率等) | ❌ | — |
| 工程度量(cycle time / lead time / WIP / 流效率) | ❌ | 统计仅到任务维度 |
| 指标口径字典与数据质量监控 | 🟡 | 报告写清口径,无字典与监控 |

#### ⑨ 搜索与知识 —— 25%

| 功能 | 现状 | 说明 |
|---|---|---|
| 关键字搜索 | ✅ | pg_trgm + GIN,命中标题/描述/编号/人名 |
| 全文检索(分词/高亮/附件内容) | ❌ | 数据量上升后体验显著下降(见第 6 章基线) |
| 高级筛选与保存视图 | 🟡 | 6 维筛选任意叠加 ✅;保存为视图 ❌ |
| 全局搜索(跨实体) | ❌ | — |
| 相似 / 重复任务识别 | ❌ | pg_trgm 已具备相似度基础 |
| 知识沉淀(SOP / FAQ) | ❌ | — |

#### ⑩ 用户体验 —— 17%

| 功能 | 现状 | 说明 |
|---|---|---|
| 基础体验(空态/加载态/错误态/相对时间/幂等提交) | ✅ | 含通知面板与详情抽屉的失败态处理 |
| 多视图切换(列表 / 看板 / 甘特 / 日历) | 🟡 | 仅列表 + 月历 |
| 拖拽操作(看板流转 / 甘特改期) | ❌ | — |
| 关注 / 订阅(watchers)与 @提及到组 | ❌ | 通知仅覆盖创建人与处理人 |
| 只读分享链接(受限免登录查看) | ❌ | — |
| 保存视图与个性化首页 | ❌ | — |
| 深色模式与键盘快捷键 | ❌ | — |
| 移动端(PWA / 小程序 / 企微内嵌 H5) | ❌ | — |
| 无障碍(WCAG AA) | ❌ | — |

### 2.3 领域 C:平台与生态

#### ⑪ AI 与智能化 —— 0%

| 功能 | 现状 | 说明 |
|---|---|---|
| 智能摘要(任务/评论串/自动周报) | ❌ | 需 LLM 接入与数据脱敏前置;单次调用即可产出,ROI 最高 |
| 自然语言建任务 | ❌ | 一句话解析出标题/处理人/截止/优先级,须弹窗确认后落库 |
| 会议纪要与邮件转任务 | ❌ | 从文本抽取行动项(负责人/期限/优先级),批量确认后建单 |
| 任务拆解建议 | ❌ | 父任务生成子任务草案(受类型/模板约束),人工增删后生效 |
| 相似 / 重复任务语义去重 | ❌ | pg_trgm 已有字符相似基础;语义层需向量检索 |
| 语义搜索(自然语言检索任务/评论/附件) | ❌ | 依赖全文检索 + 向量索引,与⑨搜索模块协同 |
| RAG 知识问答(基于任务与知识库,带出处) | ❌ | 答案须附来源链接,避免"无据可依" |
| 智能指派(按负载/技能/历史) | ❌ | 现有负载快照可作输入;建议 Top3 并给出理由 |
| 进度与 ETA 预测(预计剩余时长) | ❌ | 基于相似历史任务完成时长分布 |
| 逾期与进度风险预测 | ❌ | 统计侧已有历史数据基础;高风险任务进"需关注"清单 |
| 通知降噪与聚合摘要 | ❌ | 把多条同类通知合并为一条要事摘要 |
| 自动分类打标(类型/标签/项目归属) | ❌ | 减少手工录入,可先在导入场景试点 |
| 报表叙事与归因(指标变化解释) | ❌ | 月度/周度指标变动给出可能原因 |
| 知识沉淀(已完成任务自动生成 SOP 草案) | ❌ | 需人工审核后入库,沉淀为知识库条目 |
| 评论与回复草稿生成 | ❌ | 语气/礼貌度可选,需人工确认后发送 |
| Agent 自动跟进(催办/更新进度) | ❌ | **高风险**:必须"建议 → 人工批准 → 执行",全程审计 |
| AI 权限边界与行为审计 | ❌ | 企业要求:AI 不得越权读数据、每次调用可追溯(谁/何数据/消耗) |
| 权限一致性(AI 读取受可见性约束) | ❌ | 必须复用 `applyVisibility` 收口,越权用例为验收必测项 |
| 敏感数据脱敏与数据不出域 | ❌ | 进模型前脱敏;可强制使用本地/私有模型,声明数据不用于训练 |
| AI 适配层(多模型 / 本地模型可切换) | ❌ | 统一封装 OpenAI 兼容 / Ollama / 企业私有模型,支持降级切换 |
| Prompt 模板管理(配置化 + 版本化) | ❌ | 可落在 Nacos 配置中心,便于按租户/场景调整 |
| 结构化输出校验(JSON Schema + 失败兜底) | ❌ | 生成结果校验不过则重试或降级,避免脏数据落库 |
| 异步任务编排与超时降级 | ❌ | 生成类走 MQ 异步返回 jobId;模型不可用不影响主流程 |
| 输出标注「AI 生成」与人工确认落库 | ❌ | 所有 AI 产出显式标注;写操作一律人工确认 |
| 内容安全过滤(违规/敏感输出) | ❌ | 输出侧过滤与拒答策略,避免合规风险 |
| AI 使用权限点与租户开关(useAi) | ❌ | 现有 14 权限点基础上扩展,可按角色/租户关闭 |
| 配额与成本计量(按租户/用户) | ❌ | 日调用数/token 上限 + 调用计量表,防成本失控 |
| 向量检索基础设施(pgvector / OpenSearch k-NN) | ❌ | 起步可复用 PostgreSQL(pgvector),规模化再上专用向量库 |
| AI 效果度量与反馈闭环(采纳率/评分) | ❌ | 建议采纳率、生成内容评分,用于迭代提示词与模型选择 |

#### ⑫ 平台与运维(SRE · 供应链)—— 17%

| 功能 | 现状 | 说明 |
|---|---|---|
| 可观测性(探针/指标/链路追踪/统一日志) | 🟡 | Actuator + Prometheus 端点 + traceId 贯穿 + 统一 logback ✅;监控后端与告警未部署 |
| 监控看板与阈值告警 | ❌ | — |
| SLI / SLO / 错误预算 | ❌ | 有性能基线,无 SLO 承诺 |
| 高可用(服务多副本 + 中间件集群/托管) | ❌ | 全链路单点 |
| 无状态化(附件对象存储) | ❌ | 附件落本机磁盘,无法多副本 |
| CI/CD 与 IaC | ❌ | 无流水线 |
| 备份与灾难恢复(RPO/RTO 达标并演练) | 🟡 | 脚本就绪(4 库+附件,7 天滚动);未加密、未演练、每日全量不满足 15 分钟 RPO |
| 多环境与配置治理 | 🟡 | Nacos 配置中心已用;环境隔离规范缺失 |
| 容量 / SLA 管理与压测常态化 | 🟡 | 有性能基线(P95 6ms 级);无容量模型 |
| 供应链安全(SCA / 镜像扫描 / SBOM) | ❌ | — |
| 值班与事故响应(on-call / postmortem) | ❌ | — |
| 发布治理(灰度/蓝绿/特性开关/回滚演练) | ❌ | — |

#### ⑬ 开放与生态 —— 40%

| 功能 | 现状 | 说明 |
|---|---|---|
| 开放 API + 密钥管理 | ✅ | API Key 签发/停用/重生成,限定建任务,停用即时失效 |
| API 全量开放与配额 | 🟡 | 仅开放 1 个端点,无按租户配额 |
| OpenAPI 规范与多语言 SDK | ❌ | 有 Markdown 接口文档,无机器可读规范 |
| 插件 / 扩展体系 | ❌ | — |
| 数据可迁移性(全量导出/迁移工具) | 🟡 | CSV 导出 ✅;无迁移工具与字段映射 |

#### ⑭ 产品化与运营 —— 13%

| 功能 | 现状 | 说明 |
|---|---|---|
| 许可 / 套餐 / 计费 | ❌ | — |
| 产品内公告与 release notes | ❌ | — |
| 帮助中心 / 上下文帮助 / 新手引导 | ❌ | 有 docs,但非产品内 |
| 演示沙箱与样例数据 | 🟡 | 演示账号与数据已有,未产品化 |
| 使用分析埋点(功能采纳率) | ❌ | — |
| 客户支持流程(SLA 分级 / 工单) | ❌ | — |
| 培训与上线陪跑 | ❌ | — |
| 历史数据迁移(Excel / Jira / 竞品) | 🟡 | Excel 导入 ✅;迁移工具 ❌ |

---

## 3. 成熟度总览

| 领域 | 模块 | 得分 |
|---|---|---|
| **A 组织与权限** | ① 身份与组织 | 25% |
| | ② 权限与安全 | **44%(最高)** |
| | ③ 合规与认证 | 13% |
| **B 业务与协作** | ④ 任务与工作流 | 25% |
| | ⑤ 协作与内容 | 33% |
| | ⑥ 计划与日程 | 33% |
| | ⑦ 通知与集成 | 13% |
| | ⑧ 报表与度量 | 28% |
| | ⑨ 搜索与知识 | 25% |
| | ⑩ 用户体验 | 17% |
| **C 平台与生态** | ⑪ AI 与智能化 | **0%(最低)** |
| | ⑫ 平台与运维 | 17% |
| | ⑬ 开放与生态 | 40% |
| | ⑭ 产品化与运营 | 13% |
| | **综合企业就绪度** | **23%** |

**分项解读**

| 指标 | 数值 | 含义 |
|---|---|---|
| 核心功能地基(A①② + B④~⑨,8 模块) | **28%** | 单组织内部使用已闭环,可生产运行 |
| 企业化与产品化(③⑩⑪⑫⑬⑭,6 模块) | **17%** | 组织模型、集成、运维治理、产品化是主要缺口 |
| 综合企业就绪度(14 模块) | **23%** | 与"能进企业采购清单"的距离 |

> **结论**:TaskFlow 是**功能闭环度高、工程质量扎实**的系统(权限安全 44%、开放生态 40% 为最强项),但**企业化程度偏低**。差距主要来自产品定位(PRD 明确"企业内部单机部署 + 单组织"),而非设计与实现能力。**要进企业采购清单,必须先补第 4 节 P0 项。**

---

## 4. 差距清单与优先级

### P0 门槛项(企业 IT 安全评审必问)

| # | 能力 | 现状 | 缺失后果 |
|---|---|---|---|
| P0-1 | **SSO**(企业微信/钉钉/飞书/OIDC/LDAP) | ❌ | 企业不接受独立账号体系,安全评审不通过 |
| P0-2 | **可配置工作流 + 自定义字段** | ❌ | 只能服务一个部门的一种流程,推广即需改代码 |
| P0-3 | **数据范围权限(部门树)+ 字段级权限** | 🟡 | 矩阵组织下"能看什么"无法配置,只能人工隔离 |
| P0-4 | **全量审计 + 防篡改** | 🟡 | 合规要求"谁在何时改了什么",现仅覆盖矩阵与删除 |
| P0-5 | **合规准入材料**(等保/SOC2 对齐、渗透测试报告) | ❌ | 采购与安全评审的前置条件 |
| P0-6 | **高可用 + 监控告警 + PITR 备份演练** | 🟡❌ | 无 SLA 承诺能力;每日全量不满足 15 分钟 RPO |
| P0-7 | **无状态化(附件对象存储)** | ❌ | 多副本、备份、容器重建的前提 |

### P1 效率项(决定用户是否愿意长期使用)

| # | 能力 | 现状 |
|---|---|---|
| P1-1 | 全文检索(OpenSearch)+ 保存视图 + 全局搜索 | ❌/🟡 |
| P1-2 | IM 通知 + Webhook 事件订阅 | ❌ |
| P1-3 | 工时登记与成本核算、项目集与甘特依赖 | ❌ |
| P1-4 | 批量操作、模板库、周期任务 | ❌ |
| P1-5 | 自定义报表 + 定时周报 + 阈值预警 | ❌ |
| P1-6 | 体验层:看板与拖拽、watchers/@提及、个性化首页、移动端 PWA | 🟡/❌ |
| P1-7 | 指标口径字典与工程度量(cycle time / WIP / 流效率) | 🟡/❌ |

### P2 规模化项(客户与数据量增长后才需要)

| # | 能力 | 现状 |
|---|---|---|
| P2-1 | 多租户 / 多组织隔离(`tenant_id` + MyBatis-Plus 租户插件) | ❌ |
| P2-2 | 附件版本 / 在线预览 / 水印 | ❌ |
| P2-3 | AI 能力(摘要/指派/预测,含权限边界与审计) | ❌ |
| P2-4 | CI/CD + 灰度发布 + IaC + 供应链安全扫描 | ❌ |
| P2-5 | HR/SCIM 同步、历史数据迁移工具、许可与计费 | ❌/🟡 |
| P2-6 | 合规深化(法务保全、主体删除、导出审计)、i18n/a11y | ❌ |
| P2-7 | OpenAPI 规范 + SDK + 插件体系、数据可迁移性 | 🟡/❌ |

---

## 5. 演进路线

### 批次一:企业化地基(P0,约 6 周 / 单人)

| 交付 | 技术落点 | 验收要点 |
|---|---|---|
| SSO 接入 | 网关引入 `spring-boot-starter-oauth2-resource-server`(RS256 + JWKS);auth 对接企业 IdP(OIDC);本地账号保留兜底 | 扫码登录成功;官方 `JwtDecoder` 完成校验;本地账号仍可用 |
| 数据范围 + 字段级权限 | `role_permission` 增 `data_scope`;部门表升级为树(parent_id);`TaskService.applyVisibility` 改为读配置;响应按角色裁剪字段 | 同角色不同范围产生不同可见集;越范围 2001;敏感字段按角色隐藏 |
| 可配置工作流 + 自定义字段 | 新增 `workflow_def` / `workflow_transition`;任务表加 `ext JSONB` + `field_def` | 两部门不同流程并存;非法流转仍 2002;动态字段在列表/详情/导入导出全通 |
| 全量审计 + 防篡改 | Service 层统一记录 `audit_log`(动作/实体/前后值 diff);哈希链校验 | 抽查 5 类动作均有 diff;篡改可被检出 |
| 无状态化 + 高可用 | 附件迁 MinIO/S3 + 预签名 URL;服务 ≥2 副本 + LB;PG 主从或托管、Redis 主从、RabbitMQ 镜像或托管、Nacos 3 节点 | 杀实例不中断;附件可被任一副本读取 |
| 备份与灾备 | WAL 归档 + PITR;备份加密 + 异地副本;恢复演练脚本化 | 达成 RPO ≤15min、RTO ≤2h 并留演练记录 |
| 数据规模治理 | `task` 按 created_at 分区(或复合索引 + BRIN);`task_timeline` 分区/归档;审计冷热分层 | 50 万任务下 P95 仍 ≤1s |
| 监控告警与 SLO | Prometheus + Grafana;SLI/SLO 定义与告警规则;值班与事故流程 | 看板可用;告警可触达;流程演练一次 |

**风险与依赖**:SSO 依赖企业 IdP 配合;工作流引擎改造面最大,需回归全部状态机用例;数据范围影响所有查询,必须由统一入口收口;对象存储迁移需处理存量附件。

### 批次二:效率与集成(P1,约 4 周)

| 交付 | 技术落点 | 验收 |
|---|---|---|
| 全文检索 | OpenSearch + 事件驱动索引(outbox 增加 search 消费者) | 高亮与相关度可用;延迟 <5s |
| IM 通知 + Webhook | notification 增加通道(企微机器人);对外订阅表 + 签名 + 重试 | 建任务后 IM 收到卡片;外部系统收到签名事件 |
| 工时与成本 | `worklog` 表 + 审批;按人/项目汇总 | 登记审批闭环;成本报表可导出 |
| 项目集与甘特 | `project` / `portfolio` + 任务依赖;甘特视图 | 依赖冲突校验;拖拽改期 |
| 批量与模板 | 列表多选批量操作;任务/项目模板 | 批量 100 条 <2s;模板一键生成任务树 |
| 自定义报表与周报 | 报表定义(维度/指标)+ 定时邮件 | 订阅周报自动发送 |
| 体验层 | 看板视图 + 拖拽、watchers/@提及、保存视图与个性化首页、移动端 PWA | 关键场景移动端可用 |
| 度量字典 | 指标口径字典 + 数据质量监控 + 工程度量 | 口径可查;与手工核算一致 |

### 批次三:规模化与产品化(P2,约 4 周+)

| 交付 | 技术落点 |
|---|---|
| 多租户 | 全表 `tenant_id` + MP `TenantLineInnerInterceptor`;JWT 加 `tenantId`;Redis 键与 MQ 载荷带租户;矩阵按租户存;前端租户选择/切换 |
| 附件增强 | 版本、在线预览、水印、生命周期(低频/归档) |
| AI 能力 | LLM 网关 + 数据脱敏前置;摘要/自然语言建任务/智能指派/风险预测;AI 调用审计与权限边界 |
| 交付工程化 | CI/CD(编译 + 验收套件 + 前端构建 + 镜像)、灰度发布、特性开关、IaC;SCA/镜像扫描/SBOM |
| 产品化运营 | 许可/套餐/计费、产品内公告、帮助中心与引导、埋点分析、支持 SLA、培训陪跑 |
| 生态与合规 | OpenAPI 规范 + SDK、插件体系、SCIM/HR 同步、迁移工具;法务保全、主体删除、i18n、WCAG AA |

---

## 6. 非功能基线(默认假设版,待业务确认)

> 本章把"角色画像 / 数据规模 / NFR 指标"三项前提按**中型企业内部使用**的行业常见默认值填成可执行基线。
> ⚠️ 这些数字是**假设**,用于推导架构与容量决策;业务方确认后就地替换(见 6.6)。

### 6.1 角色画像

| 角色 | 核心诉求 | 首屏关注 |
|---|---|---|
| 普通员工 | 快速看"要我做什么"、更新进度、提交验收 | 我的待办 / 今日到期 / 指派给我 |
| 团队主管 | 分派、跟踪进度、处理验收、看负载 | 团队任务、逾期清单、人员负载 |
| PMO / 项目管理者 | 跨部门视角、里程碑与依赖、统计与周报 | 项目集进展、趋势与按时完成率 |
| IT 管理员 | 账号与权限、集成、审计、SLA | 用户/角色/矩阵、审计日志、系统健康 |
| 审计 / 合规 | 追溯"谁在何时改了什么"、导出留档 | 审计检索、导出与留证 |

### 6.2 规模基线

| 维度 | 默认基线 | 备注 |
|---|---|---|
| 租户/组织 | 1 个组织,50 个部门(3 层树),6-8 个角色 | SaaS 化按 10-50 租户规划 |
| 用户 | 注册 1,000;日活 400;峰值在线 200 | 对应 P95 并发请求 50-100 |
| 任务 | 存量 50 万;年增约 15 万;日均新增 400-600 | 含子任务,顶层约占 60% |
| 任务内容 | 时间线约 300 万行;评论 20 万;附件 3 万个 | 单任务平均 6 条时间线 |
| 附件容量 | 500 GB;单文件 ≤20 MB;日均新增 2 GB | 需对象存储 |
| 通知 | 站内 2 万条/日;邮件 5 千封/日 | 峰值 10 倍(9-10 点) |
| 审计 | 保留 3 年;约 500 万行/年 | 需归档与防篡改 |
| 统计 | 日聚合 3 年 ≈ 1,100 行 | 预聚合表规模无压力 |

### 6.3 NFR 指标基线

| 类别 | 指标 | 目标 |
|---|---|---|
| 可用性 | 月度可用性 | **99.9%**(月停机 ≤43 分钟),办公时段重点保障 |
| 数据保护 | RPO / RTO | **RPO ≤15 分钟**、RTO ≤2 小时 |
| 性能 | 列表/详情 P95 | ≤1s(局域网实测 6ms,余量充足) |
| 性能 | 统计总览 P95 | ≤2s |
| 性能 | 导入 / 导出 | 导入 500 行 ≤30s;导出 1 万行 ≤30s |
| 容量 | 单租户配额 | 任务 100 万、附件 1 TB、成员 2,000、API 1 万次/日 |
| 安全 | 越权与审计 | 跨租户/越权 0 容忍;关键动作 100% 可审计 |
| 可维护 | 发布 | 滚动/灰度发布,回滚 ≤15 分钟 |

### 6.4 基线对架构的强制要求

| 基线项 | 强制要求 | 当前状态 |
|---|---|---|
| 50 万任务 + 日增 500 | `task` 按月分区或复合索引 + BRIN;`task_timeline`(300 万行)分区/归档 | 🟡 仅常规索引,批次一补 |
| 模糊搜索体验 | 全文检索(OpenSearch) | ❌ 批次二执行(50 万行前可容忍) |
| 500 GB 附件 | 对象存储 + 预签名 URL + 生命周期策略;本地磁盘不可行 | ❌ 提前至批次一 |
| 200 并发 / P95 1s | 连接池与线程池调优;≥2 副本 + LB | 🟡 单实例 |
| 99.9% 可用性 | 消除全部单点:PG 主从/托管、Redis 主从、MQ 镜像/托管、Nacos 3 节点、服务 ≥2 副本 | ❌ 批次一必做 |
| RPO ≤15 分钟 | WAL 归档 + PITR(每日全量不达标) | ❌ 批次一必做 |
| RTO ≤2 小时 | 演练过的恢复手册 + IaC/镜像化环境 | 🟡 有脚本未演练 |
| 邮件 5 千封/日 | 真实 SMTP + 发信限速 + 退信处理 | ❌ SMTP 未配置 |
| 审计 3 年 / 500 万行 | 审计表分区 + 冷热分层 + 防篡改 | ❌ 批次一(审计)+ 批次三(归档) |
| 站内信 2 万/日 | 现有 MQ + 幂等消费足够;180 天约 360 万行,靠定时清理 | ✅ 机制齐备 |

### 6.5 容量与资源估算

| 资源 | 估算 | 说明 |
|---|---|---|
| PostgreSQL | 15-25 GB(3 年含时间线/审计)+ 索引;WAL 与备份另计 | 建议 100 GB 数据盘 |
| Redis | 1-2 GB | maxmemory 2 GB + LRU |
| RabbitMQ | 峰值积压 <200 MB | 设内存水位与惰性队列 |
| 应用内存 | 5 服务 × 2 副本 × 约 400 MB ≈ 4 GB | 每副本 `-Xmx384m` 起 |
| 应用 CPU | 2 副本 × 4 vCPU 足够 | 编译与导入导出为峰值场景 |
| 对象存储 | 500 GB + 备份余量 → 1 TB | 附件随任务归档转低频 |
| 推荐拓扑 | 2 台 4C8G + 托管 PG/Redis/对象存储;或 2 台 4C8G 全自建 + 3 节点 Nacos | 单机 2C2G 仅够演示 |

### 6.6 待业务确认清单

- [ ] 租户形态:单组织 / 多组织 / 对外 SaaS
- [ ] 用户规模:注册 ____、日活 ____、峰值并发 ____
- [ ] 任务规模:存量 ____、年增 ____、是否全量保留历史
- [ ] 附件:总量 ____、单文件上限 ____、是否需在线预览
- [ ] 通知量:日均站内/邮件 ____、是否需 IM/短信
- [ ] 审计保留期 ____ 年;是否需要防篡改与法务保全
- [ ] 可用性目标 ____%(99.5 / 99.9 / 99.99)
- [ ] RPO ____ 分钟 / RTO ____ 小时
- [ ] 性能目标:列表 P95 ____ ms、统计 P95 ____ ms、导入导出 ____
- [ ] 组织复杂度:部门层级 ____、角色数 ____、是否需数据范围与字段级权限
- [ ] 集成需求:SSO 类型 ____、IM ____、HR/SCIM ____、Webhook 消费方 ____
- [ ] 合规要求:等保 / SOC2 / ISO 27001 / 行业规范 ____
- [ ] 部署形态:自建机房 / 云主机 / 容器平台 / 私有化交付

---

## 7. 明确不做(避免过度设计)

| 项 | 理由 |
|---|---|
| 自研 IM / 视频会议 | 集成企业现有 IM,不自研 |
| 自研在线文档协同 | 对接 Office / 腾讯文档,不重造 |
| 全功能 BPMN 引擎(Activiti/Flowable 全家桶) | 只做"状态 + 流转 + 审批节点"的轻量可配置 |
| 多币种财务核算 | 一期只做工时与成本估算,不做财务系统 |
| 自研移动 App | 先用 PWA / 企业微信内嵌 H5 |

---

## 8. 决策建议

| 场景 | 建议 |
|---|---|
| **单组织内部使用(当前 PRD)** | 维持现状;仅补 P0-4(全量审计)、P0-6/P0-7(运维硬化 + 无状态化)三项高收益项 |
| **要进企业采购清单** | 先做 P0-1(SSO)与 P0-2(可配置工作流)——决定"能否被第二个部门/第二家公司用起来";同时备齐 P0-5 合规材料 |
| **要做对外 SaaS** | 在 P0 基础上加 P2-1(多租户:`tenant_id` + MP 租户插件 + 缓存键前缀 + 事件带租户) |
| **客户少但要求物理隔离** | 走"每客户独立部署实例":零代码改造,用运维成本换隔离,是最快上线路径 |

---

## 附录 A. 评分明细(便于复评)

| 模块 | ✅ | 🟡 | ❌ | 条目数 | 得分 |
|---|---|---|---|---|---|
| ① 身份与组织 | 1 | 2 | 5 | 8 | 25% |
| ② 权限与安全 | 2 | 3 | 3 | 8 | 44% |
| ③ 合规与认证 | 0 | 2 | 6 | 8 | 13% |
| ④ 任务与工作流 | 1 | 3 | 6 | 10 | 25% |
| ⑤ 协作与内容 | 3 | 0 | 6 | 9 | 33% |
| ⑥ 计划与日程 | 1 | 2 | 3 | 6 | 33% |
| ⑦ 通知与集成 | 1 | 0 | 7 | 8 | 13% |
| ⑧ 报表与度量 | 1 | 3 | 5 | 9 | 28% |
| ⑨ 搜索与知识 | 1 | 1 | 4 | 6 | 25% |
| ⑩ 用户体验 | 1 | 1 | 7 | 9 | 17% |
| ⑪ AI 与智能化 | 0 | 0 | 29 | 29 | 0% |
| ⑫ 平台与运维 | 0 | 4 | 8 | 12 | 17% |
| ⑬ 开放与生态 | 1 | 2 | 2 | 5 | 40% |
| ⑭ 产品化与运营 | 0 | 2 | 6 | 8 | 13% |
| **合计** | **13** | **25** | **97** | **135** | **综合 23%** |

**分项指数**:核心功能地基(A①② + B④~⑨,8 模块)= **28%**;企业化与产品化(③⑩⑪⑫⑬⑭,6 模块)= **17%**。
