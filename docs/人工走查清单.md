# TaskFlow 前端人工走查清单

> 适用版本:main @ d3eec94 之后的开发环境。先看「0.准备」再逐页走查,发现问题按「第 10 节」记录。

## 0. 准备

- 入口:浏览器打开 **http://localhost:5173**(Vite dev,已热更;改了代码会自动刷新,必要时强刷 Cmd+Shift+R)
- 服务:网关 8000 + 五个后端 + Nacos(8080 控制台免登录)已在跑

| 账号 | 密码 | 角色 | 说明 |
|---|---|---|---|
| admin | Admin@123 | 系统管理员 | **首次登录会强制改密**(见下注) |
| zhangming | TaskFlow@2026 | 任务管理员 | 演示账号,菜单应有统计 |
| zhaoqiang | TaskFlow@2026 | 任务管理员 | 同上 |
| lihua | TaskFlow@2026 | 普通用户 | 无权看统计/导出,可见性受限 |
| wangfang | TaskFlow@2026 | 普通用户 | 同上 |

> admin 首登强制改密:走查时把 admin 改成 **TaskFlow@2026**(与其他一致),走查完如需恢复出厂:
> ```bash
> PGPASSWORD=root psql -h 127.0.0.1 -U postgres -d auth_user_db -c \
> "UPDATE app_user SET password_hash='\$2y\$10\$yQBFrOqebPqT2IVrlWx2ZuxG2N9ReAO7qo05/.2NRMOS5TMg93Xk.', must_change_password=TRUE WHERE account='admin';"
> ```

---

## 1. 登录与会话

- [ ] 登录页:错密码提示「账号或密码错误」;连续错 5 次提示锁定(15 分钟后可再试)
- [ ] admin 首登出现「强制改密」弹窗 → 改成 TaskFlow@2026 → 自动退出或跳登录 → 新密码可登录
- [ ] 右上角「修改密码」入口(旧密码错误提示)
- [ ] 登出后回登录页;带过期 token 的请求应跳回登录(401 处理)
- [ ] 菜单按角色显隐:admin/任务管理员 全菜单;普通用户(lihua)**不应看到 统计总览 与 权限管理**;所有人都应看到 任务 / 日程 / 通知中心

## 2. 任务列表(/tasks)

- [ ] 左侧/顶部筛选:关键字、状态、优先级、类型、创建人、处理人、部门,可任意叠加;范围(全部/我创建/指派给我)切换
- [ ] 关键字同时能命中创建人姓名与处理人姓名(如搜「张」出张明相关任务)
- [ ] 分页 + 每页 10/20/50;总数与列表一致
- [ ] 导出 CSV(admin/taskAdmin):按当前筛选条件导出,文件名「任务导出-…csv」;普通用户看不到/被拒(3001)
- [ ] 导入:下载 xlsx 模板 → 填几行(处理人账号用 lihua/wangfang)→ 导入成功提示批次;故意填错一行 → 逐行错误表格
- [ ] 子任务行显示 **[父编号]** 前缀(如 [TSK-…]);父任务行显示子任务数量/箭头
- [ ] 悬停行出现 详情 / 更多▾(转派、优先级、到期、添加子任务等按权限出现)
- [ ] 搜索「DEMO-T001」能精确找到演示任务

## 3. 任务详情抽屉(点任意任务行/详情)

- [ ] 基本信息(标题/类型/优先级/处理人/到期/进度)展示正确;有权限时点「编辑」可改
- [ ] 状态机按钮按身份出现:处理人可见 受理/进度/提交验收;创建人可见 通过/驳回(驳回必须填原因);admin 全可做
- [ ] 操作后时间线新增对应记录(创建任务/更新进度/验收通过…)
- [ ] 评论:发一条 → 列表正序出现;**普通用户能评论可见任务**;本人或 admin 悬停可删,他人评论无删除
- [ ] 附件:上传 txt/md → 列表出现(文件名/大小/上传人);点下载内容一致;超 20MB 或 .exe 类型被前端拦截提示;删除仅上传人/admin
- [ ] 子任务清单:从父任务「添加子任务」;子任务详情「新建子任务」应被引导(不允许二级嵌套,后端 2011)
- [ ] 已归档任务:编辑/评论/附件全部只读/禁用(2005)
- [ ] 打开已被删除任务的通知 → 抽屉内显示「任务不存在或不可见」错误态(不再空白)

## 4. 日程(/calendar)

- [ ] 月历:周一起始;任务格显示优先级色点+标题;超 3 条折叠「+N 更多」
- [ ] 点日期 → 右侧「当日日程」列出全部到期任务;点任务 → 打开详情抽屉
- [ ] ‹ ›/今天 切换月份;相邻月灰显
- [ ] 普通用户(lihua):日历里不应出现与自己无关的到期任务(可见性)

## 5. 通知中心(/notifications + 顶栏铃铛)

- [ ] 顶栏铃铛:未读角标数字;**等 60 秒**看是否自动轮询更新
- [ ] 点铃铛 → 消息面板(倒序、未读加粗/圆点);点消息 → 标已读并跳任务详情
- [ ] 「全部已读」→ 角标归零
- [ ] 通知中心页:全部/未读筛选(未读计数实时);单条标已读;分页
- [ ] 点带任务号的消息 → **本页就地**打开详情抽屉;点已删任务的消息 → 错误态不空白
- [ ] 触发实测:admin 或 zhangming 把某个 DEMO 任务转派给 lihua → lihua 铃铛出现「任务转派」通知

## 6. 统计总览(/stats,admin 或 zhangming)

- [ ] 区间:历史累计/本月/本周/自定义(起止含当日)切换即刷新,显示区间说明文案
- [ ] 6 个 KPI 卡片数值合理:总数含已完成/未完成附注;逾期砖红色;P0 占比;平均完成时长(h);按时完成率(%)
- [ ] 四张图正常渲染:趋势(新建/完成双线)、状态环形(中心总数+图例占比)、优先级横向条、人员负载降序(不含 admin)
- [ ] 趋势数据:近 45 天应有连续曲线(演示数据创建于近 46 天)
- [ ] 普通用户(lihua)进 /stats → 无权限占位(菜单也不该显示)

## 7. 权限管理(/perm,仅 admin)

- [ ] 用户页:操作列 = 「编辑 + 更多▾」不再溢出;新增用户 → 弹窗展示初始密码(仅一次);编辑部门/邮箱;停用后该用户登录被拒;启用恢复;角色指派;重置密码 → 展示新密码(仅一次)
- [ ] 部门页:新增/编辑/删除(有用户的部门应删除失败或提示)
- [ ] 权限矩阵:勾选某角色某权限 → **立即生效**(用 lihua 账号刷新验证);去「审计日志」能看到刚才的变更记录(操作人/变更前后);尝试关闭 admin 的 manageUser/setPerm → 被拒绝(锁定项)
- [ ] 审计日志页:时间倒序、按操作人筛选;刚才的权限变更在列

## 8. 统计口径抽查(可选)

- [ ] 找一个「P0 且未完成」的 DEMO 任务(用 DB 查或在 P0 筛选下找 doing/new/wait)→ 统计页 P0 卡应包含它;该 P0 已完成则不计入(PRD 口径)

## 9. 已知环境现象(不是 bug,确认符合预期即可)

- SMTP 未配置 → 每次任务动作邮件发送失败,产生 `mail.failed` 站内告警给 admin(设计行为);admin 通知列表会有这类历史记录
- 每日 09:00 逾期扫描会向 19 条逾期演示任务的处理人/创建人发提醒;每小时整点扫描“即将到期”任务
- 压测/历史遗留:任务 235 条(含 DEMO 228),通知中心 admin 名下可能有 mail.failed 历史噪音

## 10. 问题记录格式

```
页面/路径:____
操作步骤:1. ____ 2. ____
实际结果:____(附截图)
期望结果:____
严重度:高(阻断)/中(功能错)/低(样式文案)
```
走查完把记录发回,我再逐条处理。
